ge(procs):
    #     p = mp.Process(target=worker, args=(1,))
    #     a, p = Pipe()
    #     a.send(10)
    #     # p.recv()
    #     p.start()
    #     jobs.append(p)
    # for j in jobs:
    #     j.join()